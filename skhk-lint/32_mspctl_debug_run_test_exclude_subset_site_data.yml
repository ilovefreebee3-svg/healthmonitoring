---
- name: Set variables on an imported playbook
  ansible.builtin.import_playbook: subr.local_init.yml

- name: How to run mspctl debug run --test_exclude_subset=hosting_pe
  hosts: calmcvm
#  hosts: pccvm
  gather_facts: true
  serial: 1

  vars:
    log_local_enabled: false
    log_ext_enabled: false
    log_github_enabled: false
    read_from_file: false
    logd_enabled: false
    role_enabled: false
    log_filesystem_enabled: false
    description: mspctl debug run --test_exclude_subset=hosting_pe
    idx: idx098
    role: r_mspctl_debug_run_test_exclude_subset
    vpc: tbd

  tasks:
    - name: 1. Set csv_filename
      ansible.builtin.set_fact:
        csv_filename: "{{ hostvars['localhost']['csv_filename'] }}"

    - name: 2. Display csv_filename
      ansible.builtin.debug:
        msg: "{{ hostvars['localhost']['csv_filename'] }}"

    - name: 3. Run mspctl debug run --test_exclude_subset
      ansible.builtin.command:
        argv:
          - /bin/bash
          - -c
          - |
            source /etc/profile
            mspctl debug run --test_exclude_subset=hosting_pe
      register: input_filter
      changed_when: false
      failed_when: false
      when: not (read_from_file | bool)

    - name: 4. Read from hardcoded file
      ansible.builtin.set_fact:
        input_filter:
          stdout: "{{ lookup('file', 'data.32_mspctl_debug_run_test_exclude_subset_site_data.txt') }}"
          stdout_lines: "{{ lookup('file', 'data.32_mspctl_debug_run_test_exclude_subset_site_data.txt').splitlines() }}"
          rc: 0
          failed: false
          changed: false
      when: read_from_file | bool

    - name: 5. Display the raw script output
      ansible.builtin.debug:
        var: input_filter.stdout_lines

    - name: 6. Display the raw script output
      ansible.builtin.debug:
        var: input_filter.stdout

    - name: 7. Display the stderr
      ansible.builtin.debug:
        var: input_filter.stderr

    - name: 8. Remove ANSI color codes
      ansible.builtin.set_fact:
        xinput_filter_msg: >-
          {{
            input_filter.stdout_lines
            | map('regex_replace', '\x1B\[[0-9;]*[A-Za-z]', '')
            | list
          }}

    - name: 9. Display cleaned output
      ansible.builtin.debug:
        var: xinput_filter_msg

    - name: 10. Display input_filter.stdout_lines
      ansible.builtin.debug:
        msg:
          - "V: {{ input_filter.stdout_lines }}"

    - name: 11. Display Xinput_Result msg
      ansible.builtin.debug:
        msg:
          - "X: {{ xinput_filter_msg }}"

    - name: 12. Initialize variables
      ansible.builtin.set_fact:
        current_name: ""
        failures: []
        test_results: []

    - name: 13. Parse in ONE loop (correct pairing)
      ansible.builtin.set_fact:
        current_name: >-
          {{
          subset_line.split(':', 1)[1] | trim
          if subset_line is search('^Test Name:')
          else current_name
          }}
        test_results: >-
          {{
          test_results + [{'name': current_name, 'status': subset_line.split(':', 1)[1] | trim}]
          if subset_line is search('^STATUS:')
          else test_results
          }}
        failures: >-
          {{
          failures + [{'name': current_name, 'status': subset_line.split(':', 1)[1] | trim}]
          if (subset_line is search('^STATUS:') and (subset_line.split(':', 1)[1] | trim) != 'SUCCESS')
          else failures
          }}
      loop: "{{ xinput_filter_msg }}"
      loop_control:
        loop_var: subset_line

    - name: 14. Compute summary
      ansible.builtin.set_fact:
        num_failure: "{{ failures | length }}"
        remark: >-
          {{
            failures
            | map(attribute='name')
            | zip(failures | map(attribute='status'))
            | map('join', ' -> ')
            | join(' -- ')
          }}
        answer: "{{ 'STATUS Not Success' if (failures | length) > 0 else 'STATUS SUCCESS' }}"

    - name: 15. Display remakr and answer
      ansible.builtin.debug:
        msg:
          - "Failures: {{ num_failure }}"
          - "Remark: {{ remark }}"
          - "Answer: {{ answer }}"

    - name: 97. Log to local
      ansible.builtin.import_tasks: subr.log_local.yml
      when: log_local_enabled | bool

    - name: 98. Log to external
      ansible.builtin.import_tasks: subr.log_ext.yml
      when: log_ext_enabled | bool

#    - name: 99. Log to local
#      ansible.builtin.import_tasks: subr.log_github.yml
#      when: log_github_enabled | bool
