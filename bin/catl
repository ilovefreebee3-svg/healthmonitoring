#ls -t | head -n 1 | xargs cat

#LATEST_FILE=$(ls -t *.csv | head -n 1)
#echo "--- File: $LATEST_FILE ---"
#cat "$LATEST_FILE"

f=$(find . -maxdepth 1 -type f -name "*.csv" -printf '%T+ %p\n' | sort -r | head -n 1 | cut -d' ' -f2-) && echo ">>> Showing: $f" && cat "$f"

